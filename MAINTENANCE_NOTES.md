# 最新版维护记录

本版本参考 GitHub 公开项目 `santifer/career-ops`、`DaKheera47/job-ops`、`Gsync/jobsync` 的岗位状态跟踪、截止提醒、来源可靠性和链接健康检查思路，未复制第三方代码。

## 本次改动

- 智业新版详情路由使用 `/campus/detail?jobAdId=<数字>`；采集结果优先使用 API 返回的 `JobAdId`。
- 历史 UUID 形式智业链接在 Viewer 打开时会有限时查询并自动转换；网络失败时保留原链接。
- Viewer 增加截止范围快捷筛选：全部、7 天内、已过期。
- 账号期限、个人使用许可和导出限制继续生效。

## 许可边界

岗位信息仅用于个人求职，不得转售、批量导出、公开发布或反向抓取；商业化应采用限期个人使用授权，并保留来源和隐私声明。
